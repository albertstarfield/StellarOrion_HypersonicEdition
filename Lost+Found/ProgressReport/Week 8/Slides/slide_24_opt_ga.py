def get_slide_data() -> dict:
    return {
        'title': 'Phase 4: Evolutionary GA Steering + Final Validation',
        'content': '\n# Phase 4: Evolutionary GA Steering via MoP\n\nWith the surrogate trained, we run **20,000 generations of random evolutionary search**, scored by the MoP in milliseconds rather than hours.\n\n## The GA Loop\n\n```python\nfor generation in range(20000):\n    # 1. Sample a random candidate from the search bounds\n    test_row = [random(p_min, p_max) for each active_param]\n\n    # 2. Query the MoP surrogate (instant)\n    preds = model(test_row)  # → [goal, β, q̇, Q, t_pk, g, p_stag, T_back]\n\n    # 3. Compute physics-constrained cost\n    p_mop = 0\n    if preds[7] > 350.0:  p_mop = 1e15   # T_backface constraint\n    if preds[5] > 25.0:   p_mop = 1e15   # G-load constraint\n\n    beta_penalty   = ((preds[1] - β_target) / 10.0) ** 2\n    objective_cost = ((preds[0] - goal_target) / 1.0) ** 2\n    total_cost = objective_cost + beta_penalty + p_mop\n\n    # 4. Track global minimum\n    if total_cost < min_total_cost:\n        best_config = test_sample_dict\n        min_total_cost = total_cost\n```\n\n**Stagnation early-stop**: If no improvement for 5 consecutive 1,000-generation windows, search terminates.\n\n## Cost Function Decomposition\n\n$$\\mathcal{L}_{\\text{total}} = \\underbrace{\\left(\\frac{\\hat{y}_{\\text{goal}} - y^*}{1.0}\\right)^2}_{\\text{Objective}} + \\underbrace{\\left(\\frac{\\hat{\\beta} - \\beta^*}{10}\\right)^2}_{\\text{Aero penalty}} + \\underbrace{p_{\\text{MoP}}}_{\\text{Physics kill switch}}$$\n\nWhere $p_{\\text{MoP}} = 10^{15}$ if **any hard constraint is violated**.\n\n## Phase 5: Final SPARTA Validation\n\nThe best configuration found by the GA is then **validated with a real SPARTA simulation**:\n\n```\nHIAD_GeometryEngine.py → HIAD_final.surf/.step/.stl\nrun_sparta_simulation(best_config, surf_name="HIAD_final")\nvisualizer.generate_plots() → thermal_map_opt.png, pressure_map_opt.png\nvisualizer.export_upscaled_vtk() → web/assets/data/upscaled_final.vtk\n```\n\n## Complete Pipeline Summary\n\n```\nmain.py --optimize\n ├─ Phase 0: search_map (8 design variables, 4 active)\n ├─ Phase 1: Baseline SPARTA + PINN refinement\n ├─ Phase 2: N × LHS samples → SPARTA → flight metrics → DB\n ├─ Phase 3: PyTorch MoP training (128-128 MLP, 500 epochs)\n ├─ Phase 4: 20k-gen GA on MoP (physics penalty scoring)\n └─ Phase 5: Final SPARTA validation of best_config\n```\n'
    }
