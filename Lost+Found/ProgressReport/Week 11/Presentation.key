#!/bin/sh
# Presentation.key — Interactive Shell Shim Launcher for StellarOrion Presentation
DIR="$(cd "$(dirname "$0")" && pwd)"
echo "[*] Launching StellarOrion Presentation via Presentation.key shim..."
exec python3 "$DIR/Presentation.py" "$@"
