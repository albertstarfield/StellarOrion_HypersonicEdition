def get_slide_data() -> dict:
    return {
        'title': 'Phase 3: MoP Surrogate Metamodel (PyTorch)',
        'content': '\n# Phase 3: Methodology of Physics (MoP) — Surrogate Metamodel\n\nOnce all LHS SPARTA simulations are complete, we train a **deep surrogate model** (MoP) on the collected data. This replaces expensive DSMC simulations during the evolutionary search phase.\n\n## Architecture\n\n```python\nmodel = nn.Sequential(\n    nn.Linear(n_dim, 128),   # n_dim = 4 (active design variables)\n    nn.ReLU(),\n    nn.Linear(128, 128),\n    nn.ReLU(),\n    nn.Linear(128, n_out)    # n_out = 8 output metrics\n)\n```\n\n**3-layer MLP** — simple but sufficient given the small sample count.\n\n## Input / Output\n\n**Inputs** ($\\mathbf{x} \\in \\mathbb{R}^{n\\_dim}$):\n\n$$[D,\\; \\theta,\\; N_{toroids},\\; R_n]$$\n\n**Outputs** ($\\hat{\\mathbf{y}} \\in \\mathbb{R}^{8}$):\n\n| Index | Metric | Unit |\n|---|---|---|\n| 0 | Optimization goal (drag or heat) | N or W/m² |\n| 1 | Ballistic coefficient $\\beta$ | kg/m² |\n| 2 | Peak stagnation heat flux $\\dot{q}_{stag}$ | W/m² |\n| 3 | Total heat load | J/m² |\n| 4 | Time of peak heating | s |\n| 5 | G-load | g |\n| 6 | Stagnation pressure | Pa |\n| 7 | Backface temperature $T_{back}$ | K |\n\n## Training Loop\n\n```python\noptimizer = optim.Adam(model.parameters(), lr=0.005)\nfor epoch in range(500):\n    optimizer.zero_grad()\n    loss = nn.MSELoss()(model(X_tensor), Y_tensor)\n    loss.backward()\n    optimizer.step()\n    if loss.item() < 1e-6: break  # Early stop on convergence\n```\n\n**Device priority**: MPS (Apple Silicon) → CUDA → CPU\n\n## Why a Surrogate?\n\nEach SPARTA simulation takes **minutes to hours**. The MoP maps from design space to output metrics **instantly** — enabling 20,000 candidate evaluations per second in Phase 4, which would be physically impossible with direct DSMC.\n\n$$\\underbrace{\\text{N SPARTA sims}}_{\\text{hours of compute}} \\Rightarrow \\underbrace{\\text{MoP train}}_{\\text{seconds}} \\Rightarrow \\underbrace{\\text{20k MoP evals}}_{\\text{milliseconds}}$$\n'
    }
