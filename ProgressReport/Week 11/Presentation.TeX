#!/bin/sh
# Presentation.tex — Interactive Shell Shim Launcher for StellarOrion Presentation
DIR="$(cd "$(dirname "$0")" && pwd)"
echo "[*] Launching StellarOrion Presentation via Presentation.tex shim..."
exec python3 "$DIR/Presentation.py" "$@"
